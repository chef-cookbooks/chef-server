#!/bin/sh
PATH=<%= ENV['PATH'] %><% if node['languages']['ruby']['gems_dir'] %>:<%= node['languages']['ruby']['gems_dir'] %>/bin<% end -%><% if ENV['GEM_PATH'] %>:<%= ENV['GEM_PATH'] %>/bin<% end -%>
exec 2>&1
exec /usr/bin/env chef-server -N -p <%= node['chef_server']['api_port'] %> -e production -P <%= node['chef_server']['run_path'] %>/server.%s.pid
